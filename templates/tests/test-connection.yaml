apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "german-currency.resource-name" . }}-test-connection"
  labels:
    {{ include "german-currency.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "german-currency.resource-name" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
